namespace ModularStoryFlow.Runtime.Conditions
{
    // Kept as a separate script file so Unity can bind this type as a ScriptableObject asset.
    public sealed partial class StoryStateEqualsConditionAsset
    {
    }
}
